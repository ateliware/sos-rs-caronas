from django.apps import AppConfig


class AddressManagerConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.address_manager"
    verbose_name = "Gerenciador de Endereços"
